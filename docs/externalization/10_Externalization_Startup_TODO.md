# AutomaticWriter 外販化 着手順 TODO

この TODO は、外販化を進めるうえでの唯一の実行順ドキュメントです。  
実装しただけでは完了にせず、ユーザー確認と承認をもって完了扱いにします。

---

## ステータス定義

- `[ ]` 未着手
- `[-]` 実装済み
- `[?]` 確認待ち
- `[x]` 承認済み

---

## 運用ルール

- コード実装後は、必ず確認方法と合格条件を残す
- ユーザー確認が終わるまでは `[x]` にしない
- 外販化は `品質安定化 -> クライアント差分設計 -> monorepo 基盤 -> apps 分離` の順で進める
- 手動テストは `03_AutomaticWriter_Test_Procedures.md` を参照する

---

## 現在の実施状況

### 承認済み

- build 安定化

### 実装済み

- `packages/types` の最小切り出し
- `packages/config` の最小切り出し
- workspace 基盤の最小追加
- 見出し保持の実装
- client モード向けファクトチェック強制化の実装

### 次に確認すべきもの

- 見出し保持の回帰確認
- client モードの自動ファクトチェック確認
- 自動修正後の構造確認
- クライアント固有設定の整理

---

## Phase 0. 前提整理

- [ ] monorepo 全面移行をいきなり始めない
- [ ] いきなり複数クライアント用アプリを量産しない
- [ ] クライアント差分と共通機能の境界を先に固める

完了条件:

- 品質が最低限安定している
- 実装コストを見積もれる
- 外販後の保守負荷が読める

---

## Phase 1. 外販前の品質安定化

### 1-1. build 安定化

- [x] `npm run build` が完走する
- [x] Windows 環境での build 異常終了を切り分ける
- [x] `npx tsc -b` と `vite build` を分けて確認できる
- [x] Node 24 環境でも build できるように、`vite build` を Node 22 で実行するフォールバックを入れる
- [x] ユーザー環境で `npm run build` が通ることを確認

確認方法:

1. ターミナルで `npm run build` を実行する
2. エラー終了しないことを確認する
3. `build/` 配下に成果物が生成されることを確認する
4. Vite 警告が chunk size 警告のみであることを確認する

合格条件:

- コマンドが最後まで通る
- `build/index.html` と `build/assets/` が生成される
- `dynamic import will not move module into another chunk` 警告が再発しない

備考:

- `Some chunks are larger than 700 kB` は残っているが、現時点では後続タスク扱いとする

### 1-2. 見出し保持の回帰確認

- [-] Step3 で追加した見出しが Step4 の本文生成に渡るように実装する
- [-] 「まとめ」セクション補正で、追加した末尾セクションを壊しにくいように調整する
- [ ] 対話型記事生成で追加見出しの手動確認を実施する
- [ ] 自動記事生成への影響が出ていないか回帰確認を実施する

確認待ち:

- [?] 対話型テストで追加見出しが本文に残る
- [?] `03_AutomaticWriter_Test_Procedures.md` の該当手順で再現確認できる

確認方法:

1. 対話型記事生成を開く
2. Step3 で見出しを 1 つ追加する
3. Step4 で本文生成を実行する
4. 追加した見出しが本文に残っていることを確認する
5. 「まとめ」の位置と末尾セクションの順序が崩れていないことを確認する

合格条件:

- 追加見出しが欠落しない
- 見出し順が outline と一致する
- 「まとめ」の調整で追加見出しが消えない

### 1-3. ファクトチェック強制化

- [-] client モードで常時有効化する実装を入れる
- [-] 抽出件数上限の初期値を引き上げる実装を入れる
- [-] client モードで自動ファクトチェックを走らせる実装を入れる
- [ ] client モードで設定 UI の表示が想定通りか確認する
- [ ] 自動修正後も記事構造が壊れないか手動確認する

確認待ち:

- [?] client モードで自動ファクトチェックが動くことを確認する
- [?] 自動修正後の本文が最低限読める状態を保つことを確認する

確認方法:

1. `.env.local` に `VITE_DEPLOYMENT_MODE=client` を設定する
2. 設定画面を開き、ファクトチェック設定 UI を確認する
3. 記事生成を実行する
4. 生成後にファクトチェックが自動実行されることを確認する
5. 自動修正 ON 時に本文更新後の構造を確認する

合格条件:

- client モードで無効化トグルが出ない
- 記事生成後に自動ファクトチェックが走る
- 自動修正後も見出しと本文構造が崩れない

---

## Phase 2. クライアント差分設計

### 2-1. クライアント固有値の定義

- [ ] `11_Client_Specific_Configuration_Matrix.md` を埋める
- [x] ロゴは共通扱いにする
- [x] アプリ名は共通扱いにする
- [x] 配色は共通扱いにする
- [x] URL は導入クライアントごとに Vercel で個別設置する
- [x] Supabase 接続先は導入クライアントごとに分離し、当社側の Supabase Organization で管理する
- [x] WordPress 接続先は導入クライアント内の投稿先サイトごとに複数登録・切り替えする
- [ ] 通知先
- [ ] パスワード
- [ ] deployment mode
- [ ] 表示機能 / 非表示機能
- [ ] 初期設定時に誰が触るかを定義する

用語メモ:

- 導入クライアント: AutomaticWriter を契約・利用する HP 業者 / Web 制作会社 / Web 運用会社
- 投稿先サイト: 導入クライアントが管理する顧客の WordPress サイト
- WordPress 接続先は「導入クライアントごとに 1 つ」ではなく、「投稿先サイトごとに複数登録」する

Supabase 運用メモ:

- 本番外販では、導入クライアントごとに Supabase Project を分ける
- Supabase アカウント / Organization / 請求は当社側で管理する
- 当社が初期設定、Edge Functions、DB設定、環境変数連携、保守を行う
- Supabase 利用料は販売価格または保守費に含め、導入クライアントへ別請求しない

### 2-2. 共通機能の定義

- [ ] 記事生成
- [ ] ファクトチェック
- [ ] WordPress 連携
- [ ] スケジューラー
- [ ] 共通 UI
- [ ] 設定画面

完了条件:

- 「共通 package に入れるもの」と「app 側に残すもの」の一覧が確定する

---

## Phase 3. monorepo 基盤

### 3-1. workspace 基盤

- [-] `pnpm-workspace.yaml` を追加
- [-] ルート `package.json` を workspace 前提に調整
- [-] `tsconfig.base.json` を追加
- [ ] `pnpm` を正式採用するか、`npm` のまま進めるかを決める

### 3-2. 最初に切り出す package

- [-] `packages/types` の入口を追加
- [-] `packages/config` の入口を追加
- [ ] `packages/utils`
- [ ] `packages/types` への import 置換を本格化する
- [ ] `packages/config` への import 置換を本格化する

完了条件:

- 追加した package が空振りでない
- 影響範囲が把握できる
- 既存 app が壊れずに動く

確認待ち:

- [ ] 設定と型定義を package import で読めることを確認する
- [ ] 既存 app で相対 import と package import が混在しても致命的問題がないことを確認する

---

## Phase 4. 親アプリを成立させる

### 4-1. `apps/master`

- [ ] 既存アプリを `apps/master` へ移す
- [ ] 既存 import を workspace package ベースへ置換する
- [ ] `apps/master` 単体で `dev` が通る
- [ ] `apps/master` 単体で `build` が通る

完了条件:

- 親アプリだけで現状機能が維持される

---

## Phase 5. 子アプリ雛形を作る

### 5-1. `apps/client-template`

- [ ] 親アプリからクライアント差分だけ抜いた雛形を作る
- [ ] 差分が乗る想定のロゴ、配色、環境変数ファイルを分ける
- [ ] `.env.example` をクライアント雛形向けに調整する

完了条件:

- テンプレートから複製して起動確認できる

---

## Phase 6. テスト用子アプリを 1 つ作る

### 6-1. 検証用クライアント app

- [ ] `apps/clients/client-a` をテンプレートから作成する
- [ ] クライアント設定差分だけ変えられる
- [ ] 親側の共通修正が子へ反映されることを確認する

完了条件:

- `master` と `client-a` の両方が動く

---

## Phase 7. Vercel 運用確認

### 7-1. Vercel プロジェクト分離

- [ ] `master` 用 Vercel プロジェクトを作る
- [ ] `client-a` 用 Vercel プロジェクトを作る
- [ ] app ごとに root directory を設定する
- [ ] 環境変数を app ごとに分けて登録する

完了条件:

- `master` と `client-a` が別 URL で動く

---

## Phase 8. 外販運用の型を作る

### 8-1. 新規契約時フロー

- [ ] テンプレート複製手順を整理する
- [ ] 初期設定シートを作る
- [ ] Vercel 設定手順を整理する
- [ ] 納品前チェックリストを整理する

### 8-2. 納品物一覧

- [ ] 管理者向け手順書
- [ ] 初期設定手順書
- [ ] ロールバック手順書

---

## 次に着手する項目

1. [ ] `10` の未反映箇所を閉じて、見出し保持と client モードの回帰確認を進める
2. [ ] `11_Client_Specific_Configuration_Matrix.md` を埋めてクライアント固有値一覧を確定する
3. [ ] `packages/utils` を切る
4. [ ] `packages/types` / `packages/config` の import 置換を進める
5. [ ] `apps/master` を作る

---

## 進捗判定

現時点では monorepo 実装開始前の状態として、以下を基準にする。

- [x] build 安定化
- [ ] 品質確認が最低限完了している
- [ ] クライアント差分設計がある
- [ ] 外販前チェックリストの Phase 1 が埋まっている

---

## 現在のまとめ

### すでに入っているもの

- build の安定化実装
- `packages/types` と `packages/config` の最小切り出し
- workspace 基盤の追加
- 見出し保持の実装
- client モード向けファクトチェック強制化の実装

### まだ次にやるべきもの

- 見出し保持の回帰確認
- クライアント差分一覧の確定
- `packages/utils` の切り出し
